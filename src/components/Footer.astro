<footer>
  <div class="footer-inner">
    <span class="copy">&copy; {new Date().getFullYear()} Bryan Ward</span>
    <div class="social-links">
      <a href="https://github.com/wardbryan3" target="_blank" rel="noopener noreferrer">GitHub</a>
      <a href="https://linkedin.com/in/bryanward" target="_blank" rel="noopener noreferrer">LinkedIn</a>
    </div>
  </div>
</footer>

<style>
  footer {
    position: sticky;
    bottom: 0;
    z-index: 100;
    background: color-mix(in srgb, var(--bg) 85%, transparent);
    backdrop-filter: blur(10px);
    border-top: 1px solid var(--border);
    padding: 1rem 1.5rem;
    margin-top: auto;
  }

  .footer-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 0.5rem;
  }

  .copy {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text);
  }

  .social-links {
    display: flex;
    gap: 1rem;
  }

  .social-links a {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text);
    transition: color 0.2s, text-shadow 0.2s;
  }

  .social-links a:hover {
    color: var(--accent);
    text-shadow: 0 0 6px var(--accent-glow);
  }

  @media (max-width: 768px) {
    footer {
      display: none;
    }
  }
</style>
