1. Update my actual Resume
2. Implement more real information about me throughout.

3. Overhaul folder app. It needs its forward, back, and up arrow icons updated. It should also work more like a functional folder explorer. (Ability to move through a file tree) We will add some pictures, other documents, and more fun easter eggs.
4. Add cat pictures

5."About Me" system info window
A new window (launchable from Dock or command palette) that displays a live system-info dashboard — not the terminal's fastfetch output, but a dedicated GUI panel showing "Uptime" (session duration), "Memory" (JavaScript heap usage), "Network" (connection type), and a live clock with timezone. Implementation: new SysInfoWindow.jsx pulling from navigator APIs and performance.memory. Low risk, high charm.
6. Image optimization pipeline
Add @astrojs/image or use Astro's built-in <Image /> component to optimize tux.svg and all desktop icons. Currently, SVGs and PNG icons are served raw. Benefits: reduced page weight, automatic responsive image variants, lazy loading. Risk: requires configuration and testing with current build process.
7. Blog post reading progress + estimated read time
Add a thin progress bar at the top of blog posts showing how far the user has scrolled, and display estimated read time in the post header. Implementation: IntersectionObserver + a fixed-position progress bar in BlogPost.astro. Low risk, common UX pattern that improves the blog reading experience significantly.
